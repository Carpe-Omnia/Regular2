{
  "name": "NoMouse",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd regular2 && npm install && npm run build && cd ..",
    "deploy": "cp -a regular2/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'regular2 built!'"
  }
}
